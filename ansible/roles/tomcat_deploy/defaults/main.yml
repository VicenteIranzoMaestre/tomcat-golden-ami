# Existing generic inputs (still supported as fallback)
tomcat_war_src: ""
tomcat_war_url: ""
tomcat_war_checksum: ""

# New per-version inputs (preferred)
tomcat9_war_src: "{{ playbook_dir }}/simple-app-tomcat9.war"
tomcat9_war_url: ""
tomcat9_war_checksum: ""

tomcat10_war_src: "{{ playbook_dir }}/simple-app-tomcat10.war"
tomcat10_war_url: ""
tomcat10_war_checksum: ""

# Behavior toggles
stop_before_deploy: false
clean_exploded_on_update: true
restart_on_deploy: true   # (kept via handler)

tomcat_webapps_dir: ""     # let the role autodetect if you don't set it
tomcat_service_name: ""    # let the role autodetect if you don't set it

# Optional healthcheck after deployment
# healthcheck_enabled: false
# healthcheck_url: ""           # if empty we compute http://localhost:8080/<context>/
# healthcheck_status_code: 200
# healthcheck_retries: 30
# healthcheck_delay: 2
